import React from "react";
import styles from "./ProjectCard.module.scss";

const ProjectCard = ({ post, handler, ...props }) => {
  console.log(post);
  return <div></div>;
};

export default ProjectCard;
